# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# Environment (matched at any depth)
.env
.env.*
!.env.example

# Editor / OS
.claude/settings.local.json
.vscode/
.idea/
.DS_Store
Thumbs.db

# A clone of this repo made inside itself (git clone run from the project
# directory). Harmless on disk, but it must never be committed as a gitlink.
/DPDPA-Sentinel/
