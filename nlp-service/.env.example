# Origin(s) permitted to call this service. Only the backend should.
ALLOWED_ORIGINS=http://localhost:4000
LOG_LEVEL=INFO
# Set true to download/load models at startup instead of on first request.
NLP_WARM_START=false
